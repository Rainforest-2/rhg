# BCU ability source evidence

Date: 2026-06-05.

This file records local BCU source evidence inspected for current parity work. It is not a completion claim by itself; rows still need JS runtime wiring, deterministic tests, ZIP evidence, and docs status updates before they can move to `code-complete` or `human-visual-review-needed`.

This 2026-06-05 update expands the earlier 2026-06-03 source table for remaining partial / blocked areas. It records BCU holder, runtime owner, timing/order, and implementation-readiness evidence for the next implementation pass. Runtime code was not changed by this evidence update.

## Evidence table

| Area | BCU source file / class / method | Field / key | Runtime owner / state transition | Effect owner / cleanup | JS status after this pass |
|---|---|---|---|---|---|
| `P_DELAY` holder | `battle/data/DataEnemy.java` `fillData`; `util/Data.java` `Proc.DELAY`; `util/lang/Editors.java` `DELAY` | Enemy CSV `ints[111] -> DELAY.prob`, `ints[112] -> DELAY.strength`; `Proc.DELAY.type` is proc-object/editor data, not a direct BC enemy CSV column in `DataEnemy.fillData` | `EEnemy.processProcs` accepts only when `line != -1`, `basis.est.num[line] >= 0`, and `basis.est.rem[line] > 0`; accepted strength accumulates into `status[P_DELAY][type]` | `EEnemy.processProcs` adds `EAnimCont(pos, currentLayer, A_E_DELAY DEF, -50f)` when strength is nonzero | `human-visual-review-needed`: parser/runtime/effect/tests exist; exact browser appearance not manually reviewed |
| `P_DELAY` player cooldown path | `battle/entity/EUnit.java` `processProcs` and `postUpdate`; `battle/ELineUp.java` `delay`; `battle/StageBasis.java` `getDelayStrength` and update loop | Target `getProc().IMUDELAY`; `status[P_DELAY][type]`; `basis.cdDelay[index[0]][index[1]][type] += status` | `StageBasis.update` calls `EUnit.postUpdate`, then flushes accumulated `cdDelay` once through `ELineUp.delay`; `ELineUp.delay` clamps cooldown to max and zero | Same `A_E_DELAY` actor effect at proc acceptance; cooldown visual state is BCU UI state and not a separate battle entity in current JS | `human-visual-review-needed`; JS now queues same-tick delay and flushes once in `proc-resolve` |
| `P_DELAY` enemy stage-line path | `battle/entity/EEnemy.java` `processProcs` and `postUpdate`; `util/stage/EStage.java` `delay`; `battle/StageBasis.java` update loop | `status[P_DELAY][type]`; `basis.lineDelay[line][type] = status` | `StageBasis.update` calls `EEnemy.postUpdate`, then flushes `lineDelay` once through `EStage.delay`; `EStage.delay` applies `getDelayStrength(rem, max(respawn_0, respawn_1), delay)` and clamps `rem >= 0` | Same `A_E_DELAY` actor effect at proc acceptance | `human-visual-review-needed`; JS queues same-tick delay and applies aggregate to stage row `nextFrame` |
| `IMUDELAY` resistance | `battle/entity/EUnit.java` / `EEnemy.java` `processProcs`; `EEnemy.getResistValue`; `EUnit.getResistValue`; `util/Data.java` `SUPER_SAGE_RESIST_TYPE` | `getProc().IMUDELAY.mult`, `smartImu`; no direct `DataUnit`/`DataEnemy` BC CSV column found in inspected constructors | Full immunity skips delay; partial resistance multiplies delay strength before status accumulation; sage resistance applies to `IMUDELAY` unless attacker has `AB_SKILL` | Full immunity uses invalid effect, not `A_E_DELAY`; invalid effect exact alias is outside this P_DELAY pass | Supported via `BcuResistRuntime` / `BcuDelayRuntimePatch`; direct BC CSV holder remains absent in inspected BCU source |
| `A_E_DELAY` visual | `util/pack/EffAnim.java` `EffAnimStore.A_E_DELAY`; `EUnit/EEnemy.processProcs` | `./org/battle/s23/skill_recast_decrease_e`, `skill023.png`, `skill023.imgcut`, `DefEff.DEF` | Spawn timing is proc acceptance, before postUpdate aggregate flush | `basis.lea.add(new EAnimCont(pos, currentLayer, ..., -50f)); basis.leaSort = true` | Added `effect:wave` alias `enemy-delay/*`, deterministic coordinate trace uses actor priority scale and `bcuSmokeYOffset: -50` |
| Burrow holder | `battle/data/DataEnemy.java`; `util/anim/AnimU.java` | Enemy CSV `ints[43] -> BURROW.count`, `ints[44] / 4 -> BURROW.dis`; animations include `BURROW_DOWN`, `BURROW_MOVE`, `BURROW_UP` | Runtime owner is `Entity`, not a standalone proc-status effect. `Entity.update2()` calls `startBurrow()` before normal attack start when contact/base-distance conditions hold. `startBurrow()` decrements count, sets `bdist`, sets `status[P_BURROW][2]`, and enters negative `kbTime` burrow phases. `Entity.update()` routes `kbTime < 0` through `updateBurrow()` instead of normal KB. | Entity animation owns the visible down/move/up lifecycle. No separate ZIP micro-effect is proven for normal burrow. | `ready-for-implementation after test-first`: parser exists, but JS still lacks lifecycle, hidden/touchability, collision/capture, movement, base clamp, and interaction tests. |
| Burrow phase / targetability | `battle/entity/Entity.java` `update`, `update2`, `startBurrow`, `updateBurrow`, `touchable`, `updateMove`; `battle/StageBasis.java` `inRange` | BCU uses negative `kbTime` phases: `-2` burrow down, `-3` underground move, `-4` burrow up. `touchable()` returns normal touch plus `TCH_UG` for down/up phases, and only `TCH_UG` for underground move. `StageBasis.inRange()` uses the target's `touchable()` mask. | Down/up phases remain partially targetable by touch masks; underground phase is underground-only. Capture/collision correctness therefore belongs in actor targetability/collision tests, not only render tests. | Entity animation; no separate effect cleanup. | Add `check-bcu-burrow-lifecycle-parity.mjs` before implementation. It must cover phase transitions, renderability, normal-target exclusion underground, `TCH_UG`, distance consumption, base clamp, death/revive/warp/soulstrike interactions. |
| Zombie corpse / soulstrike / death surge holders | `DataEnemy.fillData`; `DataUnit.fillData`; `AttackSimple.java`; `AtkModelEntity.java`; `Entity` / `EEnemy` death paths | `REVIVE` enemy indexes 45/46/47; `DEATHSURGE` enemy indexes 89-92; `DataUnit` `AB_ZKILL` index 52; `DataUnit` `AB_CKILL` index 98. `AB_CKILL` adds `TCH_CORPSE` to unit touch / attack touch. | BCU corpse state is owned by `Entity.ZombX` plus `Entity.touchable()` / `Entity.getTouch()`. `postUpdate` delegates zombie state, last-breathe/death calls `ZombX.prekill`, `updateRevive` advances corpse/revive, and `Entity.touchable()` exposes corpse touch only during the BCU corpse-show window. | Death soul and demon soul use `effect:soul`; death surge uses volcano container from demon soul frame timing; zombie revive visual is not fully traced in JS. | `needs-test-first`: core JS revive/soulstrike/death-surge pieces exist, but end-to-end corpse touch window, revive visual phase, ZK immunity, soulstrike cancellation, and death-surge cleanup are not fully tested. |
| Zombie killer / revive ordering | `battle/entity/Entity.java` `ZombX.damaged`, `ZombX.prekill`, `ZombX.doRevive`, `ZombX.updateRevive`; `battle/attack/AttackSimple.java`; `battle/attack/AtkModelEntity.java` | `AB_ZKILL` sets temporary zombie-kill state when the attacker can kill zombies; revive is scheduled only when zombie-kill did not block and revive count remains. Revive HP is `maxH * REVIVE.health / 100`; revive time is based on the minimum applicable revive timer plus `ZombieEff.REVIVE` animation length. | Death surge is rolled in `AnimManager.kill`; when death surge or mini-death-surge is accepted, demon soul is used and `AnimManager.update` fires the surge container when `soul.len() - dead == 21`. | `A_ZOMBIE` / `A_U_ZOMBIE` and soul assets are separate from normal corpse targetability. | Add tests for `REVIVE_SHOW_TIME` targetability split, ZK suppression, soulstrike cancellation of revive, death surge during corpse, mini-death-surge split, and cleanup ordering. |
| Summon proc schema | `util/Data.java` `Proc.SUMMON`; `battle/attack/AtkModelEntity.java`; `battle/attack/AtkModelUnit.java`; `battle/attack/AtkModelEnemy.java`; `battle/entity/Entity.java`; `battle/entity/EntCont.java` | `SUMMON` and `IMUSUMMON` are proc-object/custom attack holders, not normal current JS unit/enemy CSV parser fields. Fields include probability, id, form, magnification, distance/min/max layer, time/TBA, and `TYPE` flags such as animation type, ignore limit, fixed buff, same health, bond HP, on-hit, and on-kill. | Runtime owner is attack-model based. `AtkModelEntity.setProc` / `invokeLater` decide whether summon is immediate or deferred after hit/kill. `AtkModelUnit.summon` and `AtkModelEnemy.summon` create actor entities with side-specific paths. `EntCont` delays spawn tokens. `Entity.setSummon` applies summon animation types and bond relationships. | Summon may reuse warp/burrow/no-animation style startup. Existing `wave.zip` has summon-adjacent source paths, but no stable generic summon runtime alias is proven. | `blocked for direct implementation`: BCU schema/owner evidence is now sufficient, but JS lacks the custom/proc-object attack loader and actor creation contract. Do not implement from normal CSV. First implement source loading, summon target resolution, delayed `EntCont`, side-specific actor factory, `IMUSUMMON`, limits, `same_health`, `bond_hp`, and animation-type handoff. |
| Spirit holder / lifecycle | `battle/data/DataUnit.java`; `battle/StageBasis.java`; `battle/LineUp.java`; `battle/entity/EUnit.java` | Unit CSV `ints[110] -> SPIRIT.id`; constants include `SPIRIT_SUMMON_DELAY=15` and `SPIRIT_SUMMON_RANGE=150`. `LineUp` builds the spirit form. No enemy holder is proven. | Runtime owner is `StageBasis` / production state, not a normal proc status. Normal summoner spawn sets `summonerSummoned` and `spiritCooldown`; manual spirit spawn after cooldown creates one spirit per living summoner. `EUnit.isSpirit` starts attack on add, rejects incoming damage by setting/showing `P_IMUATK`, and self-kills with `KillMode.SPIRIT` after attack completion. Cleanup resets summoner/spirit flags when entities disappear. | Uses normal unit animation plus `A_IMUATK` on damage rejection; no separate spirit ZIP alias is proven. | `ready-for-implementation after test-first`: add parser and lifecycle tests for index 110, cooldown 15, range 150, one-spirit-per-living-summoner, damage rejection visual, self-kill, and cleanup. |
| Castle/base guard | `battle/StageBasis.java`; `battle/entity/ECastle.java`; `battle/entity/Entity.java`; `util/pack/EffAnim.java` | `StageBasis.activeGuard`; guard is stage/base state, not actor proc status. `EffAnim.A_E_GUARD` maps guard visual. | `StageBasis` initializes and activates guard when the guarded enemy-boss condition appears; `ECastle.damaged()` and `Entity.postUpdate()` hold base damage while `activeGuard == 1`; `StageBasis.checkGuard()` clears guard after the boss condition is gone; `ECastle.guardBreak()` / enemy-base `anim.getEff(GUARD_BREAK)` handles break visual. | `ECastle.guard` owns `A_E_GUARD` NONE/BREAK and clears when animation completes. `effect:wave` has `enemy-wave-guard/*` alias evidence, but JS runtime hook is missing. | `ready-for-implementation after test-first`: implement as `BattleScene`/`BattleBase` stage state, not actor proc status. First add tests for activation, damage hold, break, and guard phase trace. |
| Combo / orb / treasure / targetForms | `battle/BasisLU.java`; `battle/LineUp.java`; `battle/entity/EUnit.java`; `battle/entity/EEnemy.java`; `battle/Treasure.java`; `battle/PCoin.java`; trait compatibility code | Combo data comes through `BasisLU.getInc`; orb data mutates unit proc/ability state at construction; treasure and PCoin/talent state affect attack construction and damage/resistance math; `targetForms` affects special trait compatibility. | Runtime source is battle basis / lineup / entity construction, not only unit/enemy CSV parsing. `AtkModelEntity` and `AtkModelUnit` apply attack construction modifiers; `EUnit.getDamage` and `EEnemy.getDamage` read combo/orb/treasure/talent-derived state. | No general battle visual owner for these external modifiers. | `needs-loader-backed-fixtures / needs-test-first`: BCU source paths are identified. The remaining blocker is JS architecture/test data, not BCU source. Add fixture-backed combo/orb/treasure/PCoin loaders before changing `DamageAbilityResolver` broadly. |
| Toxic immunity / `IMUPOIATK` holder split | `battle/data/DataUnit.java`; `battle/data/DataEnemy.java`; `util/Data.java`; `battle/entity/Entity.java` | Unit CSV direct holder exists: `DataUnit.ints[90] -> IMUPOIATK.mult = 100`. Inspected normal `DataEnemy.fillData` maps enemy `ints[79]/[80]` to toxic attack `POIATK`, and does not expose a direct enemy `IMUPOIATK` CSV column. `util/Data.java` still defines `P_IMUPOIATK` and PCoin/custom proc mappings. | Runtime owner is `Entity.damaged`: target `getProc().IMUPOIATK.mult` blocks or reduces toxic direct damage. Full immunity shows invalid effect; partial resistance scales toxic max-HP percent before adding damage and poison visual. | Full immunity uses invalid effect; non-full accepted toxic uses `A_POISON`. | Do not invent an enemy CSV index. Implement unit direct holder and runtime/custom/PCoin holder support only when the corresponding JS source loaders exist. Enemy toxic immunity, if present, must come from proc-object/custom/PCoin source, not normal enemy CSV. |
| Bounty / money logic and visual evidence | `battle/entity/Entity.java`; `battle/entity/EEnemy.java`; `battle/entity/EUnit.java`; `util/pack/EffAnim.java`; effect bundle inventories | `Entity.damaged` stores accepted `P_BOUNTY` status on enemies. `EEnemy.kill` multiplies gained money by base drop multi, combo money increase, and `1 + status[P_BOUNTY][0] / 100`. `EEnemy.kill` also consumes bounty-orb contribution. `EUnit.postUpdate` resets bounty orb grade after use. | Runtime owner is kill/economy, not an actor visual effect. Additional source search found no dedicated `A_BOUNTY`, `A_MONEY`, or bounty-specific `EAnimCont` battle visual in inspected common evidence. Money UI/sign concepts are not battle entity effects. | Existing ZIP audit has no stable bounty/money effect alias in `status-effects.zip`, `wave.zip`, `kbeff.zip`, or `soul.zip`. | Treat bounty as logic-only unless future PC/draw-side source proves a battle visual. Next safe action is deterministic money/economy tests and optional negative-evidence docs, not visual implementation. |

## Implementation readiness summary

| Area | Readiness | Next safe action |
|---|---|---|
| Burrow | `ready-for-implementation after test-first` | Add deterministic lifecycle/capture/movement tests, then implement actor lifecycle. |
| Castle/base guard | `ready-for-implementation after test-first` | Add stage/base guard tests, then implement stage/base state and guard visual trace. |
| Spirit | `ready-for-implementation after test-first` | Add parser/production/lifecycle tests, then implement spirit production state and actor role. |
| Zombie corpse / soulstrike / death surge | `needs-test-first` | Add corpse targetability/revive/death-surge cleanup tests before behavior edits. |
| Summon | `blocked-by-js-loader-architecture` | Build custom/proc-object attack loader and actor creation contract first; BCU schema/owner evidence is sufficient. |
| Combo/orb/treasure/talent/targetForms | `needs-loader-backed-fixtures` | Add loader-backed source fixtures before resolver changes; BCU source paths are sufficient. |
| Toxic immunity / enemy `IMUPOIATK` | `split-holder-evidence` | Unit direct holder is safe; enemy direct CSV holder has negative evidence; custom/PCoin holder requires loader support. |
| Bounty/money visual | `logic-only-unless-future-visual-proof` | Implement/test economy logic; do not add a battle visual without new source/asset evidence. |

## Commands used

```bash
rg -n "getDelayStrength|P_DELAY|Proc\.DELAY|IMUDELAY|A_E_DELAY|cdDelay|lineDelay" /tmp/bcu-ref/common /tmp/bcu-ref/android
rg -n "BURROW|REVIVE|ZKILL|CKILL|DEATHSURGE|SPIRIT|SUMMON|activeGuard|targetForms|Orb|Treasure|Combo|AB_SKILL|getResistValue" /tmp/bcu-ref/common/BCU_java_util_common-*/battle /tmp/bcu-ref/common/BCU_java_util_common-*/util
rg -n "startBurrow|updateBurrow|touchable|TCH_UG|TCH_CORPSE|REVIVE_SHOW_TIME|doRevive|prekill|getDeathSurge|EntCont|setSummon|spiritCooldown|summonerSummoned|spiritSummoned|activeGuard|guardBreak" /tmp/bcu-ref/common/BCU_java_util_common-*/battle /tmp/bcu-ref/common/BCU_java_util_common-*/util
rg -n "SUMMON|IMUSUMMON|invokeLater|setSummon|EntCont|bond_hp|same_health|ignore_limit|fix_buff|on_hit|on_kill" /tmp/bcu-ref/common/BCU_java_util_common-*/battle /tmp/bcu-ref/common/BCU_java_util_common-*/util
rg -n "IMUPOIATK|POIATK|A_POISON|P_IMUPOIATK|PC_CORRES" /tmp/bcu-ref/common/BCU_java_util_common-*/battle /tmp/bcu-ref/common/BCU_java_util_common-*/util
rg -n "P_BOUNTY|BOUNTY|bounty|money|moneySign|EAnimCont|A_BOUNTY|A_MONEY|COIN|REWARD" /tmp/bcu-ref/common/BCU_java_util_common-*/battle /tmp/bcu-ref/common/BCU_java_util_common-*/util /tmp/bcu-ref/android/BCU_Android-master
unzip -l public/assets/bundles/effect/status-effects.zip
unzip -l public/assets/bundles/effect/wave.zip
unzip -l public/assets/bundles/effect/kbeff.zip
unzip -l public/assets/bundles/effect/soul.zip
```
