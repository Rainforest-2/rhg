# BCU unresolved evidence blockers

This file lists only evidence that could not be fully resolved after the 2026-06-12 docs refresh.

Rows here are blockers for `fully-complete` or broader implementation claims, not necessarily blockers for existing deterministic runtime checks. If a runtime is implemented but still lacks browser/manual visual acceptance, the row must say that explicitly instead of pretending the JS owner is missing. Because apparently documentation can lie with a straight face if nobody babysits it.

| Area | Missing evidence | Searches performed | Negative evidence | Best candidate | Required next step |
|---|---|---|---|---|---|
| BCU PC draw-side source | PC zip/source is not available in this checkout, so PC-only draw helpers cannot be cited | `find references/bcu -maxdepth 2 -type f`; `find . -maxdepth 2 -type f -name '*BCU*zip'`; BCU common/Android extraction command | only `BCU_java_util_common.zip` and `BCU_Android-master.zip` are present | common `Entity.AnimManager` and Android raw JSON for non-PC evidence | Add PC zip to `references/bcu/` before any claim that depends on PC-only draw helpers. |
| Enemy `IMUPOIATK` direct CSV holder | Direct `DataEnemy` raw index for toxic immunity/resistance is not proven | `rg -n "IMUPOIATK|POIATK|IMUPOI" /tmp/bcu-ref/common ...`; JS `rg -n "IMUPOIATK|toxic|POIATK" js scripts` | `DataUnit` index 90 maps `IMUPOIATK`; `DataEnemy` indexes 79/80 map `P_POIATK` only in inspected constructor | custom/proc-object `Proc.IMUPOIATK` holder | Inspect `CustomEntity`/proc serialization before adding any enemy CSV parser index. |
| Normal CSV summon holder | `Proc.SUMMON` holder in standard BC unit/enemy CSV is not proven | `rg -n "SUMMON|IMUSUMMON|AtkModelEntity|CustomEnemy" /tmp/bcu-ref/common ...`; Android `proc_jp.json`; JS summon search | inspected `DataUnit` and `DataEnemy` constructors do not assign `proc.SUMMON` | custom attack/proc-object loader | Keep normal CSV parser unchanged; map BCU custom/proc-object data before any `code-complete` summon claim. |
| BCU custom/proc-object summon loader | Explicit JS runtime exists, but automatic source loading for BCU custom/proc-object attack data is not implemented | same summon source searches plus JS `rg -n "summon|SUMMON|IMUSUMMON|EntCont" js scripts`; `scripts/check-bcu-summon-runtime-parity.mjs` | normal CSV holder remains negative; current runtime only consumes supplied per-hit `SUMMON` objects | custom attack/proc-object loader feeding `BattleAttackProfile` | Load BCU custom/proc-object `SUMMON` data into per-hit attack events; keep normal CSV parser unchanged unless a source holder is proven. |
| Summon stage allow/group and entry visuals | Runtime has side limit/layer/delay/bond support, but BCU `StageData.allow` group semantics and exact `Entity.setSummon(anim_type)` visual appearance are not source-backed in JS fixtures | `AtkModelUnit.summon`, `AtkModelEnemy.summon`, `Entity.setSummon`; JS `BcuSummonRuntime` | no deterministic fixture for stage `allow` group or human/manual visual review of `anim_type` 1-3 | source-backed stage allow fixture plus manual visual review | Add stage fixture for summoned enemy allow/group rules; record human review before any `fully-complete` claim. |
| Full `Trait.targetForms` fixture source | BCU branch is identified but local JS fixture data for special target forms is not proven | common `EEnemy.getDamage`, `Entity.traitCompatible`; JS `BcuTraitCompatibility`; docs partial searches | current checks assert resolver omission, not parity | targetForms data loader/fixture path | Add minimal source-backed targetForms fixtures before changing capture/damage compatibility. |
| Bounty/money visual | Kill money math is source-backed, but battle visual owner for money/bounty effect is not proven | common `EEnemy.kill`; JS bounty runtime search; ZIP audits for status/wave/kbeff/soul | no stable money-effect ZIP alias identified in inspected bundles | PC/draw-side battle UI/economy visual source, if available | Keep logic partial for visual row; add PC source or exhaustive negative visual search. |
| Zombie revive browser visual acceptance and extra/custom revive interactions | Standard deterministic zombie corpse/soulstrike/revive visual trace now exists, but human browser acceptance and extra/custom revive variants are still not recorded | common `Entity.ZombX.updateRevive`; ZIP audits; JS death/zombie searches; `scripts/check-bcu-zombie-corpse-soulstrike-parity.mjs` | mini-death-surge holder remains unproven; extra/custom revive interactions are not fixture-backed | current `BcuZombieCorpseRuntime` visual trace plus future extra/custom fixtures | Record browser review for DOWN/REVIVE phases; add extra-reviver/custom revive fixtures before broader `fully-complete` claims. |
| Mini-death-surge holder and edge cleanup | Death surge single-spawn timing is tested, but mini-death-surge normal holder and all extra/custom corpse cleanup variants are not fully proven in JS | common `AtkModelEntity.getDeathSurge`, `ZombX`, JS death runtime, death/zombie check | current standard death-surge + zombie-corpse path is covered; mini-death-surge remains intentionally partial | `BcuDeathAnimationRuntime` plus zombie revive/soulstrike patches | Add loader-backed/custom holder proof for mini-death-surge and end-to-end extra/custom lifecycle tests before behavior edits. |
| External combo/orb/treasure/talent/PCoin modifiers | Source paths are known, but JS data loaders and exact fixtures are missing for all affected families | common `LineUp`, `BasisLU`, `Treasure`, `EUnit.OrbHandler`, `PCoin`, `AtkModelUnit`; JS `DamageAbilityResolver` | resolver explicitly lists omissions and `check-ability-partial-blockers.mjs` asserts them | loader-backed battle basis model | Implement fixtures first; then wire resolver with source-backed battle basis state. |
| Castle/base guard browser visual acceptance | JS owner is no longer missing: `BcuCastleGuardRuntime` / `BattleSceneBcuCastleGuardPatch` implement `StageBasis.activeGuard`-equivalent state and `scripts/check-bcu-castle-guard-parity.mjs` covers active/hold/break behavior. Remaining evidence gap is manual browser appearance only. | common `StageBasis.activeGuard`, `ECastle`, `Entity.postUpdate`; JS guard/base searches; ZIP audit; `scripts/check-bcu-castle-guard-parity.mjs` | previous "runtime state hook is missing" blocker is obsolete | existing `enemy-wave-guard/*` hold/break phases | Record browser review for guard hold/break appearance before any `fully-complete` visual claim. |
