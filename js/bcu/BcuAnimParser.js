import { normalizeBcuText } from './BcuText.js';
export function parseAnim(text){const lines=normalizeBcuText(text).split('\n'); if(lines[0]?.trim()!=='[modelanim:animation]') throw new Error('Invalid animation header'); const tracks=[]; let i=1,maxFrame=0; while(i<lines.length){const h=lines[i++]?.trim(); if(!h||!h.includes(',')) continue; const hc=h.split(','); if(!Number.isFinite(+hc[0])) continue; const keyCount=Math.max(0,parseInt(lines[i++]||'0',10)||0); const keyframes=[]; for(let k=0;k<keyCount&&i<lines.length;k++,i++){const cc=(lines[i]||'').split(','); if(!Number.isFinite(+cc[0])) continue; const frame=+cc[0],value=+cc[1]||0,easing=+cc[2]||0,parameter=+cc[3]||0; keyframes.push({frame,value,easing,parameter}); if(frame>maxFrame) maxFrame=frame;} tracks.push({partId:+hc[0]||0,modification:+hc[1]||0,loop:+hc[2]||0,name:hc.slice(3).join(',').trim(),keyframes,rawHeader:h}); }
return {type:'animation',tracks,maxFrame};}
