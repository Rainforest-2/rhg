export class PreviewRenderer{constructor(canvas){this.canvas=canvas;this.ctx=canvas.getContext('2d');this.logicalW=1280;this.logicalH=720;this.resize();window.addEventListener('resize',()=>this.resize());}
resize(){const r=this.canvas.getBoundingClientRect(),dpr=window.devicePixelRatio||1;this.canvas.width=Math.max(1,Math.floor(r.width*dpr));this.canvas.height=Math.max(1,Math.floor(r.height*dpr));this.ctx.setTransform(this.canvas.width/this.logicalW,0,0,this.canvas.height/this.logicalH,0,0);} 
render(state){const c=this.ctx;c.clearRect(0,0,this.logicalW,this.logicalH); for(let x=0;x<this.logicalW;x+=40){c.strokeStyle=x%200===0?'#2f3b4c':'#1c2431';c.beginPath();c.moveTo(x,0);c.lineTo(x,this.logicalH);c.stroke();} for(let y=0;y<this.logicalH;y+=40){c.strokeStyle=y%200===0?'#2f3b4c':'#1c2431';c.beginPath();c.moveTo(0,y);c.lineTo(this.logicalW,y);c.stroke();}
const ox=this.logicalW*0.5,oy=this.logicalH*0.78; c.strokeStyle='#4da3ff';c.beginPath();c.moveTo(ox-14,oy);c.lineTo(ox+14,oy);c.moveTo(ox,oy-14);c.lineTo(ox,oy+14);c.stroke();
if(state.rawMode&&state.sprite){state.sprite.drawRawGrid(c,20,20);return;}
if(!state.sprite||!state.model) return; for(const p of state.model.getDrawList()){const w=p.world; c.save(); c.translate(ox+w.x*state.scale,oy+w.y*state.scale); c.rotate((w.a/3600)*Math.PI*2); c.globalAlpha=w.o??1; const part=state.sprite.imgcut.parts[p.partIndex]; if(part&&part.w>0&&part.h>0){const sx=w.sx*state.scale,sy=w.sy*state.scale; state.sprite.drawPart(c,p.partIndex,-part.w*0.5*sx,-part.h*0.5*sy,{scaleX:sx,scaleY:sy}); if(state.showBounds){c.strokeStyle='#a78bfa';c.strokeRect(-part.w*0.5*sx,-part.h*0.5*sy,part.w*sx,part.h*sy);} if(state.showPivots){c.fillStyle='#22d3ee';c.fillRect(-2,-2,4,4);} if(state.showParts){c.fillStyle='#f8fafc';c.font='11px monospace';c.fillText(`${p.index}:${p.name}`,4,-4);} } c.restore();}
}
}
