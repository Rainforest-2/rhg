export class BcuImgCut { constructor(parts = []) { this.parts = parts; this.byLabel = new Map(parts.map((p) => [p.label, p])); } static parse(text) { const lines = String(text || '').replace(/^\uFEFF/, '').split(/\r?\n/).map((v) => v.trim()).filter(Boolean); const parts = []; for (const line of lines) { if (line === '[imgcut]') continue; const cols = line.split(','); const x = Number(cols[0]); const y = Number(cols[1]); const w = Number(cols[2]); const h = Number(cols[3]); if (![x, y, w, h].every(Number.isFinite)) continue; parts.push({ x, y, w, h, label: cols.slice(4).join(',').trim(), index: parts.length }); } return new BcuImgCut(parts);} static async load(path){const r=await fetch(path);if(!r.ok)throw new Error(`imgcut fetch failed: ${path}`);return BcuImgCut.parse(await r.text());} getByIndex(i){return this.parts[i]||null;} getByLabel(label){return this.byLabel.get(label)||null;} draw(ctx,image,part,dx,dy,dw=part?.w,dh=part?.h){if(!part)return false;ctx.drawImage(image,part.x,part.y,part.w,part.h,dx,dy,dw,dh);return true;} }
