import { BATTLE_CONFIG } from '../battle/BattleConfig.js';
import { BcuImgCut } from './BcuImgCut.js';
import { BcuSpriteText } from './BcuSpriteText.js';
const loadImage=(src)=>new Promise((res,rej)=>{const i=new Image();i.onload=()=>res(i);i.onerror=()=>rej(new Error(`image load failed:${src}`));i.src=src;});
export class PlayerProductionBar{constructor({scene,mount=document.body}){this.scene=scene;this.mount=mount;this.lastTap=new Map();this.spriteText=new BcuSpriteText(console);this.setup();}async setup(){this.root=document.createElement('div');this.root.className='prod-ui';this.root.innerHTML=`<canvas class='money' width='360' height='48'></canvas><div class='cards'></div>`;this.mount.appendChild(this.root);this.moneyCanvas=this.root.querySelector('.money');this.moneyCtx=this.moneyCanvas.getContext('2d');this.cardsWrap=this.root.querySelector('.cards');this.frameImage=await loadImage('assets/bcu/000001/org/page/uni.png');this.frameCut=await BcuImgCut.load('assets/bcu/000001/org/data/uni.imgcut');this.framePart=this.frameCut.parts.find((p)=>p.w===110&&p.h===85)||this.frameCut.getByIndex(0);await this.spriteText.init();this.cards=await Promise.all(BATTLE_CONFIG.rosters.dogPlayer.map(async(d)=>{const c=document.createElement('canvas');c.width=110;c.height=85;c.className='prod-card';this.cardsWrap.appendChild(c);let icon;try{icon=await loadImage(d.uiIcon?.primary);}catch{icon=await loadImage(d.uiIcon?.fallback);}const fire=(ev)=>{ev.preventDefault();const now=performance.now();if(now-(this.lastTap.get(d.slotId)||0)<180)return;this.lastTap.set(d.slotId,now);c.classList.add('press');setTimeout(()=>c.classList.remove('press'),80);this.scene?.requestPlayerSpawn?.(d.slotId);};c.addEventListener('pointerdown',fire,{passive:false});c.addEventListener('click',fire,{passive:false});return{d,c,ctx:c.getContext('2d'),icon};}));}bindScene(scene){this.scene=scene;}update(scene=this.scene){if(!this.cards||!scene)return;this.scene=scene;const money=Math.floor(scene.economy?.money??0);const max=Math.floor(scene.economy?.maxMoney??BATTLE_CONFIG.economy.dogPlayer.maxMoney??0);this.moneyCtx.clearRect(0,0,this.moneyCanvas.width,this.moneyCanvas.height);this.spriteText.drawNumber(this.moneyCtx,`${money}/${max}`,8,8,{small:false,disabled:false});for(const it of this.cards){const s=scene.economy?.getStatus(it.d)||{};const ctx=it.ctx;ctx.clearRect(0,0,110,85);this.frameCut.draw(ctx,this.frameImage,this.framePart,0,0,110,85);ctx.drawImage(it.icon,8,8,94,62);if(s.cooldownRemainingMs>0||!s.affordable){ctx.fillStyle='rgba(0,0,0,0.45)';ctx.fillRect(0,0,110,85);}if(s.cooldownRemainingMs>0){const ratio=s.cooldownRatio??0;const bw=90,bh=10,bx=10,by=70;const rem=Math.max(0,Math.min(1,ratio))*bw;ctx.fillStyle='#6ee7ff';ctx.fillRect(bx,by,bw-rem,bh);ctx.fillStyle='#111';ctx.fillRect(bx+(bw-rem),by,rem,bh);}else this.spriteText.drawNumber(ctx,`${it.d.cost}`,56,64,{small:true,disabled:!s.affordable});}}dispose(){this.root?.remove();}}
