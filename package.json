{
  "name": "rhg-bcu-battle",
  "version": "1.0.0",
  "private": true,
  "description": "BCU-parity battle/stage/render web app bundled with Vite.",
  "license": "UNLICENSED",
  "type": "module",
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "playwright": "^1.60.0",
    "vite": "^8.1.0"
  },
  "scripts": {
    "check": "node scripts/check-bcu-ability-parity-safe-suite.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check && npm test",
    "verify:dev": "bash scripts/dev-verify-env.sh",
    "preview:stop": "bash scripts/stop-preview.sh",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "rg": "^0.0.2"
  }
}
